const Voting = artifacts.require("Voting")

module.exports = function(deployer) {
  deployer.deploy(Voting);
};

// ganache-cli -m "silver course cost salon bring front review member wall tilt absent motor"